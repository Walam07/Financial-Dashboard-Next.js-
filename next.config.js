/** @type {import('next').NextConfig} */
const nextConfig = {
  // The 'output: export' is needed for building the mobile app with Capacitor later.
  output: 'export',
};

module.exports = nextConfig;